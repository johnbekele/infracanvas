

def greet(name: str) -> str:
    """Return a friendly greeting for ``name``.

    The docstring stays with the function so retrieval can match the words a
    user will search for.
    """
    return f"hello {name}"


def unused() -> None:
    pass
