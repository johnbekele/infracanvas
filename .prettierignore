node_modules
dist
build
.turbo
coverage
pnpm-lock.yaml

# A worktree created under the repository root holds a whole second checkout of
# another branch. CI never sees one, so checking it locally reports failures from
# code that is not under review.
.claude/worktrees

# Python
.venv
uv.lock
__pycache__

# Rust
target
Cargo.lock

# Generated artefacts are formatted by their generator, and reformatting them
# here would show up as drift in Gate 4.
packages/ir-schema/generated
services/brain/src/brain/ir/generated
tests/fixtures/generated

# Golden emitter output, compared byte for byte
**/__golden__/**

# Written byte for byte by scripts/ci/build-price-snapshot.mjs, and compared
# byte for byte by its --check mode.
data/pricing/MANIFEST.json
