/**
 * The preview payload version, in its own module so that the cache keys can
 * fold it in without importing the module whose functions the cache serves.
 */
export const PATCH_PREVIEW_VERSION = 1;
